# 明日のタスク: RTK Fixed (fix=6) 到達

**日付**: 2026-07-01
**ステータス**: ⬜ 未着手

---

## 🔴 優先度1: 環境復旧（必須）

### 1-1. u-blox USB 接続確認
```bash
ls /dev/tty.usbmodem*
```
- [ ] デバイス名を確認（`/dev/tty.usbmodem113301` または新名称）
- [ ] 認識されない場合はUSB抜き差し

### 1-2. Raspi Tailscale 接続確認
```bash
/opt/homebrew/bin/tailscale ping raspi5
```
- [ ] ping 応答確認

### 1-3. GW-Mac 作業ディレクトリ確認
```bash
cd ~/GCS-UmemotoLab && source .venv/bin/activate
```
- [ ] venv 有効化確認

---

## 🔴 優先度2: パイプライン起動

### 2-1. RTK 基地局起動
```bash
nohup python rtk_tools/rtk_base_station.py \
  --serial-port /dev/tty.usbmodem113301 --tcp-port 2101 &
```
- [ ] ログで `TCP listening on 0.0.0.0:2101` 確認
- [ ] ログで RTCM フレーム受信確認

### 2-2. Bridge 起動（Tailscale直結）
```bash
python scripts/direct_bridge.py &
```
- [ ] `Bridge: UDP:14552 <-> TCP:100.123.158.105:5760` 表示確認
- [ ] GCSサーバー起動確認（自動起動のはず）

### 2-3. GCS バックエンド接続
```bash
curl -s -X POST http://localhost:8000/api/connect \
  -H 'Content-Type: application/json' \
  -d '{"config_path":"config/gcs_local.yml"}'
```
- [ ] `{"status":"connected"...}` 応答確認

---

## 🟡 優先度3: RTCM 注入 + fix 監視

### 3-1. RTCM 注入確認
```bash
tail -f /tmp/gcs_web.log | grep -E "injected|fix="
```
- [ ] `RTCM data injected: X bytes` が定期的に表示される
- [ ] GPS fix が 3 → 4 に遷移する

### 3-2. fix=5/6 監視（最重要）
```bash
tail -f /tmp/gcs_web.log | grep "fix="
```
- [ ] fix=5 (RTK_FLOAT) が出現するか監視
- [ ] fix=6 (RTK_FIXED) 出現でミッション完了！
- [ ] GPS_RTK メッセージ確認: `grep GPS_RTK /tmp/gcs_web.log`

### 3-3. QGC 補助確認
```
QGC → TCP://100.123.158.105:5760 で接続
MAVLink Inspector → "GPS_RTK" 検索
```
- [ ] GPS_RTK メッセージが受信されているか

---

## 🟢 優先度4: データ収集（fix=6 到達後）

### 4-1. サンプル5 収集
```bash
python scripts/gps_compare_collect.py \
  --ublox /dev/tty.usbmodem113301 \
  --gcs-url http://localhost:8000 \
  --output logs/compare_sample5.csv \
  --count 20
```
- [ ] 20サンプル取得、fix=6 確認

### 4-2. 報告書更新
- [ ] `docs/progress_report_20260624.md` セクション4.4 に追記
- [ ] 全サンプル比較表を更新
- [ ] セクション5.3 を「達成」に更新

---

## 🔑 覚えておくべきこと

| 項目 | 値 |
|------|-----|
| Raspi IP | `100.123.158.105` |
| Raspi port | `5760` (mavlink-routerd) |
| u-blox デバイス | `/dev/tty.usbmodem113301` |
| RTCM TCP | `127.0.0.1:2101` |
| GCS URL | `http://localhost:8000` |
| Bridge UDP | `127.0.0.1:14552` → TCP `100.123.158.105:5760` |
| GPS_TYPE | 9 (DroneCAN) — 変更しない！ |
| GPS_INJECT_TO | send to all (127) |
| GPS_AUTO_CONFIG | 2 (DroneCAN auto-config on) |
| EKF3 | healthy |

---

## ⚠️ fix=5/6 が出現しない場合のフォールバック

1. `GPS_AUTO_CONFIG=2` の確認（QGCパラメータ）
2. QGC DroneCAN メニュー → F9P ノード → `TMODE` 検索 → 0 (Disabled) 確認
3. Survey-In を 0.1m 精度に再設定
4. サンプル5として fix=4 DGPS のデータを記録し、原因調査を継続
